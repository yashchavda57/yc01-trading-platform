spring:
  application:
    name: order-service
  profiles:
    active: prod

server:
  port: 8080

logging:
  level:
    root: WARN
    com.chavd.yc01: INFO
  file:
    name: logs/trading-platform.log
    max-size: 10MB
    max-history: 30

