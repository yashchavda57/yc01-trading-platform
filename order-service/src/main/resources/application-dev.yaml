spring:
  application:
    name: order-service
  profiles:
    active: dev

server:
  port: 8080

logging:
  level:
    root: INFO
    com.chavd.yc01: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

